# termn 📝
A simple terminal-based notepad for Linux users.

## Features
- Create new text files easily
- Open and view existing text files
- Append text using `:wq` to save and exit

## Installation
### Using Make
```bash
git clone https://github.com/<your-username>/termn.git
cd termn
make
sudo make install
